---
title: "Supported emulators"
description: "Every launcher the broker registers, what it can do, and the details of how each one is driven."
---

An activate request names its launcher with the `emulator` field. Each launcher
is a subclass of the broker's `Emulator` base class that knows how to boot a
game, where the emulator keeps its save data, and whether it can save and load
state on demand.

| `emulator` | System | Save states | Disc swap | Save subtrees shipped in the archive |
| --- | --- | --- | --- | --- |
| `retroarch` | anything with a libretro core | yes, unless the platform's core opts out | yes | `states/`, `saves/` under `RETROARCH_DATA_DIR` |
| `pcsx2` | PlayStation 2 | yes | no | `memcards/`, `sstates/` |
| `duckstation` | PlayStation | no (a resume state is written at exit) | no | `memcards/`, `savestates/` |
| `dolphin` | GameCube, Wii | yes | no | `StateSaves/`, `GC/`, `Wii/` |
| `ppsspp` | PSP | yes | no | `SAVEDATA/`, `PPSSPP_STATE/` under `PSP/` |
| `cemu` | Wii U | no | no | `usr/save/` under the mlc01 tree |
| `azahar` | 3DS | no | no | the SD `title/` and `extdata/` trees and the NAND `extdata/` and `sysdata/` trees |
| `eden` | Switch | no | no | `nand/user/save/` plus the profile store under `nand/system/save/` |
| `shadps4` | PlayStation 4 | no | no | `home/1000/savedata/` |
| `rpcs3` | PlayStation 3 | no | no | the save directories under `dev_hdd0/` |
| `xemu` | Xbox | no | no | extracted from the HDD image into a staging directory |
| `xenia` | Xbox 360 | no | no | `content/` under `XENIA_DATA_DIR` |
| `scummvm` | LucasArts and Sierra era adventure games | yes, over the Global Main Menu | no | `saves/` under `SCUMMVM_DATA_DIR` |
| `flycast` | Sega Dreamcast | no (a resume state is written at exit) | no | the flat `FLYCAST_DATA_DIR` (VMU saves and the savestate) |
| `desktop` | the webstation desktop itself | no | no | none |

"Save states" is what the status response reports as `supports_states`. When
it is off, the state routes answer `400` and `resume_slot` is ignored; the
game's own save data is the only persistence. The exact paths are the
`save_subtrees` attribute of each class in the
[Python reference](/docs/developer/reference/emulators).

## RetroArch

`emulator: "retroarch"` is the general purpose launcher: one class covering
every libretro core, with `rom.platform` picking the core (ngc and wii both
map to `dolphin`, plus snes, n64, dc, saturn, psp, nds, 3ds, arcade and
genesis). Missing cores are downloaded from the libretro buildbot into the
user's own `libretro_directory`, which is where RetroArch also keeps the
matching `.info` files: a core loaded from anywhere else has no core info and
`GET_STATUS` then segfaults RetroArch mid-session. The user's config is never
edited, only layered with `--appendconfig`.

Save states are real, but RetroArch has no "save to slot n" command, so the
broker parks the slot instead: it walks down to the -1 floor and back up to
`RETROARCH_STATE_SLOT` (default 0). That runs once per launch and again only
when a save lands elsewhere, which means the player moved the slot with their
own hotkeys. The first save of a session costs about 3 s, the rest under 1 s.
Loads use `LOAD_STATE_SLOT`, which is already absolute. Tunables:
`RETROARCH_SLOT_STEP_DELAY` (0.1 s between presses) and
`RETROARCH_SLOT_HOME_STEPS` (24 presses to reach the floor).

Not every core serializes, so a platform whose core does not can set
`"savestate": false` in the table. `supports_states` then reports `false` for
that platform, the state routes answer `400`, `resume_slot` is ignored and
exit saves no state. RetroArch also empties its working slot at activate, the
same as PCSX2 below.

Multi-disc games: `rom.path` may point at an `.m3u` playlist or a folder
holding one, and the broker prefers the playlist on disc platforms. A running
core can change discs through the [swap-disc route](/docs/api/activate#swap-discs).

The platform-to-core table lives in `retroarch_platforms.json` next to the
launcher; `RETROARCH_CORE_URL_<CORE>` pins a single core's download without
editing it.

## PCSX2

Driven over its PINE socket (`$XDG_RUNTIME_DIR/pcsx2.sock`) for save, load,
status and the game serial. PCSX2 works in slot 10, its own autosave slot.
State files (`.p2s`) are named for the disc they were taken from, and the
serial only comes off the running disc, so a file left over from an earlier
session cannot be told apart from the current game's. PCSX2 therefore empties
its working slot at the start of activate, before the archive is restored;
RomM already holds those states, and clearing them is what stops the last
player's save being served as this one's.

The Slot 1 memory card is a folder card the broker owns (`PCSX2_SLOT1_CARD`,
default `romm-slot1`). With `save.memory_card_synced` set, the whole card
travels on the [memory card routes](/docs/api/archives#memory-cards) instead of
inside the save archive.

## DuckStation

No mid-session save states: DuckStation exposes nothing the broker can drive
while a game runs, so the state routes answer `400`. What it does have is a
graceful shutdown that serialises a resume state, which exit reports as its
state file; that is why `DUCKSTATION_STOP_WAIT` is the longest SIGTERM grace
of the set. Memory cards and that state live under the data root the archive
carries.

## Dolphin

Save and load go through xdotool hotkeys (Shift+F`n` saves slot `n`, F`n`
loads it), so the broker waits for the render window before sending them and
confirms a save by watching `StateSaves/` for the file. The video backend is
pinned to OpenGL at launch (`DOLPHIN_VIDEO_BACKEND`) and state thumbnails are
disabled because the GPU-rendered core crashes producing them. GameCube
Slot A is pinned to the GCI folder card device so the card is a directory the
archive can carry.

## PPSSPP

Hotkey driven like Dolphin, working in slot 1. Save data and states live under
`PSP/` in the config root.

For PSP the RetroArch `ppsspp` core is the better option. It is the same
emulator core underneath, but it comes with RetroArch's controller handling
and its real save-state interface, where the standalone is driven by hotkeys
into its own GUI and has the flakier pad support of the two. PSP is not
demanding enough for RetroArch's overhead to matter. The standalone stays
supported for anyone who wants PPSSPP's own UI and settings.

## Cemu

No save states; `mlc01/usr/save/` is the persistence. Cemu binds controllers
by SDL GUID, so the launcher derives the selkies virtual pad's GUIDs from
`CEMU_PAD_NAME` and writes a controller profile before each launch
(`CEMU_PAD_UUIDS` overrides the derived list). Cemu has no SIGTERM handler,
so `CEMU_STOP_WAIT` only covers process-group teardown.

## Azahar

No save states and no control interface, so exit is a kill and only what the
game already wrote survives. Single game launches start it windowed because
Azahar stops rendering when the display resizes underneath it in fullscreen.
For 3DS the RetroArch core is the better option and is what a `3ds` launch
uses by default.

## Eden

No save states; `nand/user/save/` is the persistence. A running game takes
longer than the base grace to tear down, hence `EDEN_STOP_WAIT`.

## shadPS4

`emulator: "shadps4"` launches the shadPS4 PlayStation 4 emulator. shadPS4
has no save states (`resume_slot` is ignored); the game's save data
under `home/1000/savedata/` is the persistence. The binary is picked from
`$HOME/.local/share/shadPS4QtLauncher/versions`: the `Pre-release/` build if
present (it always trumps releases), otherwise the newest semver release
folder (`vX.Y.Z - ... - <date>/`, each holding `Shadps4-sdl.AppImage`).
Override the search with `SHADPS4_BIN` (explicit path), `SHADPS4_VERSIONS_DIR`,
`SHADPS4_BIN_NAME`, or `SHADPS4_DATA_DIR`. The broker drives shadPS4 through
its stdin IPC (`SHADPS4_ENABLE_IPC=true`): RUN/START boot the game headlessly,
and exit sends STOP (the SDL quit event) for a graceful teardown before the
save delta is dumped. shadPS4 has no SIGTERM handler, so SIGTERM is only the
fallback if STOP doesn't finish in `SHADPS4_STOP_WAIT` (default 20 s).

`rom.path` may be a game folder (holding `eboot.bin`), a bare `eboot.bin`, or a
`.zar`. A `.pkg`, or a `.7z`/`.zip`/`.rar` holding one, boots only with
`SHADPS4_CACHE_ENABLED` on, since it has to be unpacked into the cache first;
with the cache off those formats are refused rather than accepted and failed
later, and are left out of the emulator's advertised extensions entirely.

## RPCS3

No save states; `dev_hdd0/home/` is the persistence. `rom.path` may be a
PKG, which the launcher installs first (`RPCS3_INSTALL_TIMEOUT` bounds the
decryption), an extracted game folder, or a disc image.

A `.7z`/`.zip`/`.rar` archive of a game folder boots only with
`RPCS3_CACHE_ENABLED` on, since it has to be extracted first; with the cache
off it is refused rather than accepted and failed later, and is left out of
the emulator's advertised extensions. A PKG is unaffected either way: its
installer is separate from the cache.

## xemu

No save states. xemu keeps saves inside the Xbox HDD image, so the launcher
extracts them from the qcow2 (through the raw image the container prepares on
first init) after a clean shutdown, and injects them back before launch; the
save directories are matched on the disk's own case. The renderer is pinned in
`xemu.toml` before each launch (`XEMU_RENDERER`, `KEEP` to leave it alone) and
`XEMU_SOFTWARE_GL` forces CPU rendering for hosts where both GPU paths abort.

## Xenia

`emulator: "xenia"` launches the Xenia Edge Xbox 360 emulator. Xenia has no
save states (`resume_slot` is ignored) and no control interface of any kind
(no IPC, socket or stdin protocol), so the game's own save data is the
persistence and exit is a SIGTERM, which Xenia takes as a plain kill. That is
safe because guest save writes go straight through to host files, so nothing
is lost in a buffer. The save archive is the whole `content/` tree under
`XENIA_DATA_DIR`: Xbox 360 save paths embed the profile XUID and the profile
store lives in that same tree, so the two travel together and line up on a
restore into a fresh container. `rom.path` may be an XISO (`.iso`), a
bare `.xex`, an extracted dump (a folder holding `default.xex`), or an XBLA /
Games on Demand title folder: the broker walks the console's own layout
(`[Content/<XUID>/]<TITLE_ID>/000D0000` or `00007000/<package>`) and boots the
STFS package it finds there, checked by magic so DLC, title updates and the
`.data` payload beside a GoD package are passed over.

## Flycast

No control socket, and no SIGTERM handler either, so the only clean shutdown
path is a graceful window-close request: the broker activates Flycast's own
window and sends Alt+F4, which reaches Flycast's `SDL_QUIT` handling and is
the only place a save state gets written (`FLYCAST_STOP_WAIT` gives that room
before escalating to a SIGTERM that skips the save entirely). Like
DuckStation, there is no mid-session save/load: a launch resumes from
whatever state was written on the previous clean exit, and the current
session's state is written once, on exit.

Save state is a single deterministic file, `<rom>.state`, in the flat
`FLYCAST_DATA_DIR`. VMU saves live loose in that same directory and travel
with it as one save subtree, since nothing separates them from the savestate
or a first-run onboarding artifact. A RetroArch core also covers Dreamcast
(`dc` defaults to it), for anyone who prefers RetroArch's shared save-state UI
and hotkeys instead.

## ScummVM

`rom.path` is a *folder* of game data rather than a disc image: ScummVM detects
a game by scanning a directory, so a launch registers it with
`scummvm --add --path=<folder>` and reads the generated *target* name
(`monkey`, `gob1-cd-fr`) back out of `scummvm.ini`. That target, not the path,
is what boots the game and what every save file is named after, and a folder
that gains no domain is what "nothing bootable here" means: `--add` reports
success having added nothing, so its exit code settles neither question. A
`rom.path` pointing at a file, a `.scummvm` marker for instance, registers the
folder around it.

A multilingual game folder registers one target per language ScummVM
detects in it (`gob1-cd-fr`, `gob1-cd-de`), and the target decides which
variant's resources the engine loads: `--language` alone does not reroute a
launch. The `language` on the activate payload is what picks between them, and
it also goes on the command line so the running game agrees with the domain it
booted from. Codes are translated to ScummVM's own spellings (`pt-br` becomes
`br`, `jp` becomes `ja`, `zh-hans` becomes `cn`), a close relative stands in
when the exact one is absent (a `us` variant answers an `en` request), and a
code ScummVM would reject is dropped rather than failing the boot. A rom that
names no language falls back to `gui_language`, the player's own interface
language: RomM rarely knows what language a game is in, and a multilingual
folder is exactly the case where it usually does not, so the player's is then
the only thing saying which variant they want. With neither, the pick is
alphabetical and stable, which is what keeps the `<target>.sNN` save names from
moving between relaunches.

There is no control socket and no state hotkeys, so save and load drive the
Global Main Menu with xdotool: the menu key opens it, the bare-letter Save/Load
hotkey presses the button, `Down` walks the chooser to the slot and `Return`
activates it. In save mode the first `Return` starts editing the slot's
description and a second commits it. Three things are pinned in `scummvm.ini`
to make that work at all. `gui_saveload_chooser=list`, because the default grid
chooser has no keyboard path to a numbered slot. `keymap_global_MENU`, bound to
`SCUMMVM_MENU_KEY` (default `F11`): ScummVM ships the menu on `C+F5`, and a
modifier does not survive injection into the container's Xwayland, so the key
arrives without its Ctrl and the menu never opens; the unmodified `F5` that
does work belongs to the engine keymap, which an engine may take for itself
(gob answers it with Gobliiins' own panel, which has no Save). And the button
letters follow the GUI translation (`~S~ave` becomes `~S~auvegarder`), so they
are read back from `gui_language`, which the activate payload pins when it
carries one and leaves as the user set it when it does not. Only that
one keymap action is pinned; any other binding the user set is left alone.

The macro says nothing about its result, so a save is only reported once the
slot's file has actually changed on disk. Engines without runtime save support
put up a message dialog instead, which the broker dismisses rather than leaving
the game paused behind a menu. Gobliiins is the canonical case: its gob engine
creates no save handler at all, since passwords are the game's own progression
system. Those games still ship their own saves in the archive, and a boot-time
resume still works.

ScummVM's saves *are* its states: there is no separate state format, so the
working slot and the game's own saves come out of one directory and are told
apart by filename. Slot 0 is ScummVM's autosave and its own chooser marks it
write protected (`MetaEngine::listSaves` sets the flag), so the broker never
writes there; it works in `SCUMMVM_STATE_SLOT` (default 1). A state pushed back
in is renamed onto the booted target, because ScummVM finds a save by name
alone and a multilingual folder registers one target per language.

Two more pins are for the stream rather than for ScummVM's own sake.
`fullscreen`, pinned **off** whatever the launch. A ScummVM window is its
game's own resolution, so 640x480 sits as a postage stamp in the middle of the
stream. ScummVM's own fullscreen is the wrong way to fix that: it makes
SDL grab the pointer and confine it to a rect (`shouldGrab = ... ||
fullscreenFlags`, then `SDL_SetWindowMouseRect`) and ask for an explicit
display mode, and both fight a stack that feeds an absolute pointer and owns
the display size itself: the pointer stops tracking the stream while clicks
still land, and these games are nothing but mouse.

So the window manager does it instead. After launch the window is moved to the
origin and sized to the display (`SCUMMVM_FILL_SCREEN`, on by default). SDL
reads that as an ordinary resize, scales its output into it and letterboxes to
keep the game's 4:3 on its own, and never sets the flag that triggers the grab.
It is best-effort and purely cosmetic: a display size it cannot read, or a
window that never appears, leaves the game running at its own size rather than
failing the launch. What it does not remove is the title bar, since no tool in
the image can (xdotool 3.2016 has no `windowstate`, and there is no wmctrl);
a labwc window rule would. (ScummVM's `window_maximized` is not used either:
labwc leaves the window at its own size regardless, so it fills nothing.) And
`gfx_mode=surfacesdl`, because the OpenGL renderer scales mouse coordinates
through `SDL_GL_GetDrawableSize`, which means nothing outside a GL window, so
against an injected absolute pointer the game takes clicks while the cursor
stops moving. SDL is also forced onto X11 at launch so the macros have an
Xwayland window to talk to.

None of that reaches the one pointer problem a launcher cannot fix. A running
game holds Xwayland's warp-emulation pointer lock (ScummVM hides the system
cursor and warps the pointer), and labwc drops the *absolute* motion the
browser sends while a lock is active yet still forwards relative motion, so
the in-game cursor freezes with clicks landing. The image has to patch
selkies' `input_handler.py` to send every absolute move as a relative delta
followed by the absolute position, and to read the X pointer back after a
pause (it ships `python-xlib` for that) so a warp issued by the game does not
leave a lasting offset. Without it the pointer is dead windowed too, whatever
this launcher pins.

## Desktop

`emulator: "desktop"` streams the full webstation desktop so emulators can be
configured through the GUI. No `rom` or `save`; same room interface, same exit
teardown. This is how you do the one-time [setup](/docs/emulators/setup).
